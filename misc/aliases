alias ls='LC_COLLATE=C ls -A -o -h --color=always --group-directories-first'

alias chx='chmod +x'
alias permof='stat --printf "%n - %a\n"'
alias sizeof='du -sh'
alias grep='grep --color'
alias df='df -h -T --total'
alias c='clear'
alias x='exit'

alias sr='screen -r'
alias sn='screen -S'

function gitpush()
{
    if [ -n "$1" ]; then
	git config user.email "karabaja4@gmail.com"
	git config user.name "karabaja4"
        git add .
        git commit -a -m "$1"
        git push origin master
    else
        echo "Please include a commit message."
    fi
}

mkmine()
{
    if [ -n "$1" ]; then
        sudo chown -R ${USER}:${USER} $1
    else
        echo "No file defined."
    fi
}

alias regdom='nano /etc/conf.d/wireless-regdom'
alias ifedit='nano /etc/udev/rules.d/10-network.rules'
alias speedtest='wget http://newsletter.t-com.hr/adslbrzina/random-100mb.zip'

#alias rsnc='sudo pacman -Rsnc'
#alias pacman='sudo pacman'
alias fixres='/home/igor/arch/scripts/fixres.sh'
alias ed='mousepad'
alias ws='rdesktop -g 1680x1050 -z -x m -P -k hr -r sound:off 20.0.0.108'
