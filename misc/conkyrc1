conky.config = {
	use_xft = true,
	font = 'Consolas:size=18',
	draw_shades = false,
	alignment = 'top_left',
	gap_x = 50,
	gap_y = 50,
	update_interval = 1,
	double_buffer = true,
	own_window = true,
	own_window_type = 'desktop',
	own_window_transparent = true,

};

conky.text = [[
$nodename $sysname $kernel

Uptime:  ${uptime}
CPU:     ${cpu}%
RAM:     ${mem} / ${memmax}
Disk:    ${fs_used /} / ${fs_size /}
Network: ${downspeed enp2s0} | ${upspeed enp2s0}
]];
